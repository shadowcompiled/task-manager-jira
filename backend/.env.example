# Vercel / Production (single Vercel project – see repo DEPLOYMENT.md)
POSTGRES_URL=postgres://user:pass@host:5432/db?sslmode=require
JWT_SECRET=your-secret-key-change-in-production
CRON_SECRET=random-string-for-cron-auth

# CORS (optional – backend allows https://task-manager-jira.vercel.app and *.vercel.app by default)
# For Railway/cross-origin: set if you use a custom frontend URL, e.g. https://myapp.vercel.app
# CORS_ORIGIN=https://task-manager-jira.vercel.app,https://myapp.vercel.app

# Email (optional – app works without)
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_FROM=mailto:you@example.com
# Or use SendGrid:
# SENDGRID_API_KEY=

# Push (required for push notifications)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Local dev (optional - uses POSTGRES_URL if set)
# DATABASE_PATH=./restaurant.db
